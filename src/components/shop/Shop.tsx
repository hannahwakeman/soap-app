import * as React from 'react';
import './Shop.scss';
export default function Shop(props: { isMobile: boolean; }) {
  let isMobile: boolean = props.isMobile;
  if (isMobile){}
  return <div className="about-container"></div>;
}
